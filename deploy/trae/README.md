# MCTS-TD 决策引擎 — Trae 部署说明

## 安装方式

### 方式一：项目规则

将 `instructions.md` 的内容添加到 Trae 的项目规则中。

### 方式二：自定义 AI 指令

将 `instructions.md` 的内容复制到 Trae 的自定义 AI 指令输入框。

## 文件说明

```
deploy/trae/
├── README.md              # 本说明文件
└── instructions.md        # 完整的决策引擎指令（包含所有核心规则）
```